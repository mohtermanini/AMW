import styled from "styled-components";
import { colors } from "../../variables/colors";

export const StyeldTitleBar = styled.div`
    background-color: ${colors.primary};
    padding: 2rem 5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: #fff;
`;